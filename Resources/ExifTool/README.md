# ExifTool Binary Directory

Place the ExifTool binary here before building.

Download the latest ExifTool from: https://exiftool.org/
- macOS distribution: `Image-ExifTool-XX.XX.tar.gz`
- After extracting, copy the `exiftool` perl script or the standalone binary here.

The binary should be named `exiftool` (no extension).
